package main

import (
    "fmt"
)

func readConfig() {
    // Hardcoded credentials (fake examples)
    const dbPassword = "dbsecretpassword"

    fmt.Println("Reading config with password:", dbPassword)
}
